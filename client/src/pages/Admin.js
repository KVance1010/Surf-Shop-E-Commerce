
import React from 'react'
import {useQuery, useMutation} from '@apollo/client'

const Admin = () => {
    return(
        <div>
            <h1>
                Add new item:
            </h1>
        </div>
    )
}

export default Admin